"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
	palette: {
		background: {
			paper: "#f2e9e4",
			default: "#778da9",
		},
		primary: {
			main: "#1b263b",
		},
	},
});

export default theme;
